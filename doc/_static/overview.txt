Overview
============

Sometimes it is required to take a screen shot not of the whole web page but only a part of
it. Such a screen shot can contains for example a single button or form.

Seleshot possibilities:

* Support for taking screenshot(s) of elements with a specified id(s)
* Support for taking screenshot(s) of elements with a specified xpath(s)
* Support for remote webdriver
